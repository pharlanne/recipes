<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="example.codeclan.com.recipes.RecipesActivity"
    android:background="@drawable/home"
    android:clickable="true">


    <ListView
        android:layout_width="180dp"
        android:layout_height="250dp"
        android:id="@+id/recipesListView"
        android:textFilterEnabled="true"
        android:touchscreenBlocksFocus="false"
        style="@style/AppTheme"
        android:foregroundGravity="center_vertical"
        android:layout_below="@+id/textView"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="49dp" />

    <TextView
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Bien Manger"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical"
        android:textAlignment="center"
        android:textSize="50dp"
        android:textIsSelectable="false"
        android:transitionName="@string/values"
        android:typeface="normal"
        android:textStyle="bold"
        android:textColor="@android:color/holo_green_light"
        android:autoText="true" />

    <SearchView
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:id="@+id/searchView"
        android:layout_alignParentBottom="true"
        android:layout_alignLeft="@+id/recipesListView"
        android:layout_alignStart="@+id/recipesListView"
        android:background="#e2f5ba" />

</RelativeLayout>
